@model ContosoUniversity.Models.Course
@if (ViewData["RequestedView"] == "Details")
{
    <h1 class="Course">@Model.Title</h1>
}
else
{
    <h1 class="Course">Delete @Model.Title</h1>
}

<div class="Course">
    <form method="post">
        <h4>Course</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.CourseID)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.CourseID)
            </dd>

            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Title)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Title)
            </dd>

            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Credits)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Credits)
            </dd>
        </dl>

        @if (ViewData["RequestedView"] == "Details")
        {
            <a asp-action="index" class="btn btn-primary">back</a>
            <a asp-action="Clone" asp-route-id="@Model.CourseID" class="btn btn-primary">Clone</a>
        }
        else
        {
            <input asp-action="DeleteConfirmed"
                   asp-controller="Courses"
                   type="submit"
                   class="btn btn-primary"
                   value="Delete" />
            <a asp-action="index" class="btn btn-primary">Cancel</a>
        }
    </form>
</div>